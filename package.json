{
  "name": "weather-app",
  "version": "1.0.0",
  "description": "Weather App using Electron",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "dotenv": "^16.0.3",
    "electron": "^23.0.0"
  },
  "devDependencies": {
    "@electron/packager": "^18.3.6",
    "electron-builder": "^26.0.12"
  }
}
